<div>
  
    <h1><%= link_to @student.first_name, student_path(@student) %></h1>
    <h1><%= link_to @student.last_name, student_path(@student) %></h1>
    <p> This student is currently <%= @student.active ? "active" : "inactive"%>.</p>>

</div>

